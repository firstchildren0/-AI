import React from "react";
import ReactDOM from "react-dom/client";
import DiagnosisQuizApp from "./quiz";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DiagnosisQuizApp />
  </React.StrictMode>
);